# TASKS

One task per cycle. PROGRAM.md picks the top unchecked `- [ ]` under `## Open`. Local tasks have no `#<number>` prefix; GH-backed ones do.

---

## Open

---

## Known Limitations

- GH Actions build fails to push to ghcr.io — needs package write permission in GitHub package settings
- Scroll depth on Planning (12.3 screens), Tax (6.8), Transactions (7.7), Settings (5.9), Analysis (4.8) exceeds 3-screen target — requires sub-tabs or collapsible sections

---

## Completed

- [x] Materialized view `current_holdings` refresh contract locked — 16 unit tests in `internal/handler/current_holdings_refresh_test.go` pin the post-import refresh wiring + the MV aggregation math. Wiring: import.go calls RefreshCurrentHoldings AFTER insertTransactions; failures are logged as WARNING (not writeError — the import succeeded, only the cache is stale); settings.go's manual rebuild path also triggers a refresh; queries.sql uses `REFRESH MATERIALIZED VIEW CONCURRENTLY` (which requires the UNIQUE index pinned in migration 002). Aggregation: empty input → no rows; buy adds qty and folds fee into cost basis; partial sell weights cost basis (`(buy_amt+buy_fee − sell_amt+sell_fee)/net_qty`); full divestment dropped by HAVING > 0; transfer_out subtracts like sell; transfer/savings_plan add like buy; dividends roll into total_dividends without polluting qty; cash transactions (NULL ISIN) excluded; grouping is per-(account_id, security_isin); post-import scenario combines pre-existing + new transactions correctly across both an existing position (cost basis re-weighted) and a brand-new ISIN.
- [x] FX ECB weekend fallback + cross-currency round-trip locked — added `market.LookupRateForDate(rates, currency, target)` in `internal/market/ecb.go` that walks back to the most recent rate ≤ target date (ECB doesn't publish on weekends/holidays, so Saturday/Sunday/holiday lookups naturally resolve to the prior Friday). 12 unit tests in `internal/market/fx_fallback_test.go` pin: Saturday → Friday rate; Sunday → Friday rate; exact weekday returns same day; holiday Monday walks back to Friday; date before any rate returns 0; unknown currency returns 0; sparse currency (GBP gap) doesn't leak across boundaries; plus round-trip stability USD→EUR→USD, GBP→EUR→GBP, and the cross-currency loop USD→EUR→GBP→EUR→USD across magnitudes 0.01 to 1M (all within 1e-9 relative epsilon). Documents the ECB units-per-EUR convention (amountEUR = amount / rate).
- [x] Teilfreistellung classifier + known-ISIN verification locked — added `analytics.TeilfreistellungRate(equityPct)` pure function in `internal/analytics/tax.go` mapping equity exposure → InvStG 2018 partial-exemption rate (≥51% → 30% Aktienfonds, ≥25% → 15% Mischfonds, else 0% Sonstige), plus a new `TeilfreistellungBond=0.00` constant. 4 unit tests in `internal/analytics/teilfreistellung_test.go` pin: the three rate constants; bucket boundary semantics (51% and 25% are inclusive lower bounds, 50.99% and 24.99% fall into the lower bucket); a curated set of 15 well-known German-traded ISINs (iShares Core MSCI World/EM/S&P500/Europe, Vanguard FTSE All-World + LifeStrategy 80/60/40/20, Xtrackers MSCI World, iShares + Vanguard + Xtrackers bond ETFs, Xetra-Gold) routing to the correct bucket per their fact-sheet equity %; and documents the current legacy gap (handler-side `equityMap[isin] = asset_class == "etf"` over-claims 30% for bond ETFs) with `TeilfreistellungRate` as the migration target for ~15 call sites in analysis.go/portfolio.go/scheduler/jobs.go.
- [x] TWR vs IRR divergence regression locked — 5 unit tests in `internal/analytics/twr_irr_regression_test.go` pin the contract that TWR (manager skill, timing-neutral) and IRR (investor experience, money-weighted) diverge under mid-period contributions: control case with single deposit converges to TWR≈IRR≈10%; mid-period contribution before a gain → TWR=50% but IRR≈70% (capital deployed early earns more weight); bad timing (deposit right before a loss) → TWR=+20% but IRR<0 (investor loses money despite positive manager performance); good timing (deposit right after a loss) → TWR=−40% but IRR>0 (investor profits despite poor manager performance); two portfolios with same sub-period returns + different deposit dates → identical TWR but spread IRR (late > early). Pinning the sign convention (TWR: +deposit; IRR: −deposit, +terminal liquidation) also catches sign-flip refactors.
- [x] Login autofocus + tab order locked — 5 unit tests in `internal/handler/login_focus_order_test.go` pin Login.tsx focus management: exactly one `autoFocus` attribute, positioned between the username opening tag and the password field; DOM source order username < password < Sign In < Sign in with Passkey (browsers tab in DOM order when no `tabIndex` is set); zero `tabIndex` attributes anywhere (the natural-order anti-pattern guard); password input opening tag explicitly does NOT carry autoFocus (defense in depth); the passkey button lives OUTSIDE the `<form>` (so Enter in password can't trigger it) but is still a native `<button>` so it stays in the natural tab sequence.
- [x] Passkey button visibility locked — 6 unit tests in `internal/handler/login_passkey_visibility_test.go` pin Login.tsx: initial `useState(false)` (no flash before detection runs); useEffect-on-mount keys off `window.PublicKeyCredential` (canonical WebAuthn probe — drift to `navigator.credentials` would render in browsers that have Credential Management but not WebAuthn); exactly one `setPasskeyAvailable(true)` call lives inside that check; no `setPasskeyAvailable(false)` setter (relies on initial state); JSX guard wraps BOTH the "or" divider AND the passkey button in one fragment, so a refactor that splits the conditional can't leave a lonely separator under unsupported browsers.
- [x] Login submit gate + Enter-to-submit contract locked — 5 unit tests in `internal/handler/login_form_test.go` pin Login.tsx: submit disabled iff `loading || !password` (single char enabled, whitespace-only enabled because JS treats " " as truthy, username intentionally NOT part of the gate); password input controlled-bound to setPassword so the gate responds to typing; structural assertions read Login.tsx and lock the keyboard-accessibility contract by asserting `<form onSubmit={handleSubmit}>` + `<button type="submit">` + `e.preventDefault()` are present (native HTML form spec then guarantees Enter inside any input dispatches submit); and the disabled-attr lives on the Sign In button (not the Passkey button which has its own gate).
- [x] Build commit + date footer contract locked — 7 unit tests in `internal/handler/buildinfo_test.go` pin the wiring chain Dockerfile ARG → ldflags var → /api/settings/build-info JSON → Settings.tsx footer: commit truncates to 7 chars (no-op for Makefile `git --short` path, real truncation for GH Actions full 40-char SHA); footer hides iff commit is empty or "unknown"; date sub-line hides iff built_at is empty or "unknown"; backend default "dev" leaks through to the footer in raw `go build` mode (intentional — locks the dev-tag visibility); JS `toLocaleDateString('de-DE', {day:'2-digit', month:'2-digit', year:'numeric'})` produces "DD.MM.YYYY" with dot separators on leap-day and normal dates.
- [x] Notification channel create/delete contract locked — 9 unit tests in `internal/handler/channels_test.go` pin HandleCreateChannel + HandleDeleteChannel: type ∈ {email, ntfy, pushover, webhook} (case-sensitive); blank name defaults to type; channel_for ∈ {all, alerts, digest} defaults to "all"; digest_frequency ∈ {weekly, monthly, quarterly, never} defaults to "monthly"; config nil → `{}`; config must JSON-unmarshal into a map[string]any (rejects array/scalar string/scalar number/unbalanced/empty — documented quirk: `null` is silently accepted because Go unmarshals it into a nil map); config size capped at 4096 bytes (4096 accepted, 4097 rejected); delete-path UUID validation. Pins the validation gate that's drifted before (case-sensitivity of channel_for, JSON-object check added retroactively).
- [x] Generate Last Month wealth report cycle locked — 10 unit tests in `internal/handler/reports_test.go` pin the per-period contract that "Generate Last Month" depends on: monthly bounds (first→last day, label `YYYY-MM`, leap-year correctness for 2020/2024/2100); annual bounds (Jan 1→Dec 31, label `YYYY`); validation rejecting bad report_type / out-of-range year / month outside [1,12]; the frontend's `new Date(now.year, now.month-1, 1)` rolls year back when fired in January; NW aggregation picks oldest snapshot in range as start, newest as end; change_pct stays 0 (not NaN/Inf) when nwStart=0; dividend sum filters to dividend-type AND in-range; per-ISIN weight/returnPct rounding with zero-cost guard; top gainer/loser identification by max/min returnPct; PDF filename format `wealth-report-{period_label}.pdf` (the `<a download>` link target).
- [x] Passkey enrollment lifecycle locked — 7 unit tests in `internal/handler/passkey_test.go` pin the authorization + lifecycle contract around the WebAuthn ceremony (which itself needs a real authenticator and is exercised by the go-webauthn library). Covers: post-Finish credential appears in HandleListPasskeys; empty `?name=` defaults to "Passkey"; HandleListPasskeys + HandleDeletePasskey strictly scope by `user_id` so user A cannot enumerate or delete user B's passkeys (the `WHERE id=$1 AND user_id=$2` clause); cross-user delete is a 0-row no-op; deleting a nonexistent ID doesn't disturb adjacent rows; HandleFinishRegistration refuses without a prior Begin (single-use session, LoadAndDelete); session storage is keyed by userID so B can't consume A's session.
- [x] TOTP enrollment cycle locked — 6 unit tests in `internal/auth/totp_test.go` pin: `totp.Generate` returns a scannable `otpauth://totp/` URL containing issuer=Wealth + account name + ≥16-char base32 secret; `EncryptSecret`/`DecryptSecret` AES-256-GCM round-trip with `enc:` prefix; a freshly generated `totp.GenerateCode` validates against the same secret; wrong/empty/non-numeric codes all rejected; full end-to-end Setup→Verify cycle (generate → encrypt → decrypt → validate correct code, then reject `000000`); legacy plaintext secrets (no `enc:` prefix) still pass through `DecryptSecret` for migration. Pins `pquerna/otp/totp` API contract so a future cipher or library swap can't silently break 2FA.
- [x] Admin gate + create-user validation locked — 10 unit tests pin: blank role defaults to "member"; "admin" accepted; unknown roles (superadmin, Admin, MEMBER, owner) rejected; username + password both required; password ≥8 chars (7 rejected, 8 accepted). requireAdmin gate: admin allowed, member denied 403, auth-disabled bypass, no-session bypass, unknown user denied 403.
- [x] Add Account reactive fields verified — "Employer Match" appears iff `newTaxTreatment === 'bav'`; "Stock ISIN" appears iff `newInstitution === 'morgan_stanley'`. Conditions are independent (MS + bAV shows both). Typed values survive hide→show cycles because state lives on the parent. 7 ui-auditor steps all PASS.
- [x] Export Transactions round-trips cleanly — added `WealthExportParser` that recognizes the BOM-prefixed `date;type;account;counterparty;security_isin;quantity;price;amount;fee;tax;currency;reference` header from `HandleExportTransactions` and parses each row into a Transaction with US-locale decimal parsing. Registered in the parser dispatch list. 4 tests cover round-trip field accuracy, re-import idempotence (identical hashes), cross-account distinct hashes, and Detect() rejecting Sparkasse/N26 headers.
- [x] Settings "Refresh Prices" button cycle verified — click triggers "Refresh Prices" → "Running..." → "Started" (within ~11ms of POST resolving) → scheduler-status re-fetched at t=3s with advanced timestamp → button reverts to "Refresh Prices" at t=5s. Full cycle well under the 30s spec budget; POST returns 200 with `{"status":"started"}`; background job runs asynchronously via `sched.RunPriceUpdate()`.
- [x] Transactions type badge mapping + WCAG cleanup — text mapping was 10/10 spec-correct but `bg-{color}/10` produced no class against var-backed tokens, so the chip lost its pill affordance. Migrated to `bg-parchment-deep + border border-{color} + text-{color}` outlined-chip style; fee unchanged at `bg-divider + ink-body`. Also darkened light-mode `--color-slate` #6B7080 → #5E626C (and the matching useThemeColors fallback) so the savings_plan badge text clears WCAG AA cleanly (5.51:1 vs. 4.45:1 before). Fixed stale amber fallback in useThemeColors.ts too. Dark mode + chart palette unchanged.
- [x] Transactions pagination contract verified — `pages = ceil(total/limit)`, `currentPage = floor(offset/limit) + 1`; Previous disabled at offset===0; Next disabled when offset+limit >= total; pagination block hidden when pages <= 1. ui-auditor confirmed all 5 properties on live (total=101, limit=50 → "Page 1 of 3") + mocked totals (25 → no pagination; 100 → divisible-by-limit edge; 125 → mid-page Next behavior).
- [x] Transactions filter contract locked — 7 unit tests cover the HandleList filter logic: type is exact match (no prefix matching → "savings" doesn't hit "savings_plan"); date lower bound inclusive (from=date keeps the row); date upper bound inclusive via end-of-day expansion (to=date keeps a txn with afternoon time component); both bounds combined yield [from, to] inclusive; type + date combine with AND semantics; search is case-insensitive across Counterparty/Reference/Account/ISIN.
- [x] Morgan Stanley cash-neutral invariant verified — 3 tests: net cash flow ≈0 USD across all real MS fixtures (Releases / Releases Net Shares / Withdrawals); every sell pairs 1:1 with an auto-wire withdrawal of equal amount and same order ref; re-importing each report yields byte-for-byte identical hash sequences; synthetic test confirms explicit "Cash" plan rows in the Withdrawals Report are dropped (double-debit guard).
- [x] CSV import idempotence locked — parser-level tests verify each supported importer (Sparkasse, N26, Scalable, Revolut Current, ING Ordermanager, ING Depotübersicht, Delta, Revolut Savings) produces stable ImportHash values on re-parse, plus hashes are unique within a single parse AND differ across distinct accounts. DB unique constraint on import_hash then guarantees no duplicate rows on re-upload. Trade Republic / DKB / comdirect parsers not yet implemented — out of scope for this iteration.
- [x] /planning page heading — added `<h1>Planning</h1>` (font-serif text-title text-ink) at the top of the planning tab block, gated on `isEmbedded` so it only appears when reached via /planning (not when toggling tabs in-page from /). Verified at 1280x800 and 375x812: heading present on /planning, not on /, no "Net Worth" string in headings of /planning.
- [x] /planning hero + top tab bar suppression verified — NetWorth.tsx uses `isEmbedded = !!defaultTab` to skip both the TabBar and the Total Net Worth hero block when called with `defaultTab="planning"`. Live verified at 1280x800 and 375x812: hero absent, no Overview/Projection/Accounts tablist, planning content begins with FIRE Calculator; navigating back to `/` restores both.
- [x] Cross-broker note: fires whenever income spans ≥2 institutions — previously only fired on a gain/loss split (loss-offset opportunity), but spec wants the note any time the user must reconcile Sparerpauschbetrag across banks. Now tailored messaging: loss-offset wording on gain/loss split (higher-value action), FSA-reconciliation wording otherwise (mentions `N Banken` count). 6 unit tests pin all branches.
- [x] Anlage KAP reconciliation locked — 7 unit tests pin the per-broker invariants: equity ETF dividends get 30% Teilfreistellung; single-stock dividends and interest do NOT; equity gains get 30% TFS but losses are preserved at full magnitude (otherwise users would lose offset capacity); cross-broker reconciliation (A's 2000 EUR gain + 300 EUR div, B's 1000 EUR loss + 80 EUR interest → net taxable 690 EUR, FSA absorbs it → 0 tax); TeilfreistellungEquity constant pinned at 0.30.
- [x] Tax Calendar urgency bands — replaced static `urgencyColors` map (backend qualitative urgency) with time-based bands: past → muted+60% opacity, <14d → claret, <60d → amber, ≥60d → divider/ink-muted; dropped broken `bg-amber/10` alpha modifier for `bg-inset + border-l-[3px]`; date label now includes `· {N}d` / `today` / `past`. Live verified: 2 past + 3 distant cards at exact band colors; <14d / <60d code paths inspected (no events in those windows today).
- [x] Spending: subscriptions detection + windowed savings rate — added `savings_rate_pct_12m` (trailing 12-month window) alongside the existing lifetime rate; frontend headline KPI now uses the windowed value with lifetime as a sub-line when they differ. 8 unit tests pin subscription detection (freq ≥ 3, amount in [1, 500], annual = freq×amount until freq≥8 → extrapolates to monthly) and the windowed savings rate (24-month recent improvement reflected; fallback to all months when N<window; zero-income returns 0; negative rate when expenses exceed income).
- [x] Concentration thresholds locked — 7 boundary tests pin <5% no alert, 5.0% / 7.5% / 9.99% warning (amber), 10.0% / 35% critical (claret); spec-aligned check on the ConcentrationWarningThreshold constant so a tweak in portfolio.go surfaces in CI before drifting away from the frontend chip colours.
- [x] Overlap heatmap symmetry + diagonal — HandleOverlap now reuses `analytics.BuildOverlapMatrix` (same code path already covered by Table tests); added 5 explicit property tests asserting matrix[i][j]==matrix[j][i] and matrix[i][i]==100 across single ETF / two ETFs / five ETFs asymmetric mix / identical holdings / empty input.
- [x] Allocation Unknown bucket — HandleAllocationSummary / HandleSectors / HandleCountries route value with missing or partial sector/country metadata into an "Unknown" bucket so each allocation map sums to the portfolio's total. Currencies already cleanly handled via ComputeCurrencyExposure. 5 unit tests pin: full coverage / one-holding-no-metadata / partial coverage / all-unknown / zero-or-negative-value-skipped.
- [x] Sell Simulator Kirchensteuer support — extracted `SellTaxRate(churchTaxRate)` formula = 0.25 × (1 + Soli + KiSt); SimulateSell takes the rate; handler reads `?church_tax=` query; frontend adds a Kirchensteuer dropdown (none / 8% BY/BW / 28.625% other) with localStorage persistence. 9 unit tests pin the rate composition (clamp at 0 / 0.09) and end-to-end SimulateSell math (equity TFS + non-equity no TFS + loss-yields-zero-tax + 8/9% church surcharge).
- [x] Tax-loss pots equity/general separation locked — 5 analytics unit tests verify ComputeLossPots keeps the German Aktienverlusttopf and allgemeiner Verlusttopf strictly one-way: equity loss cannot reduce dividend/interest/general gain; general loss cannot offset equity gain; Teilfreistellung 30% applies to equity P&L; carry-forward works per pot independently; non-tax txn types are ignored.
- [x] FSA utilization supports joint filing — HandleFSAStatus accepts `?joint=1` and doubles allowance from 1000 → 2000; response gains `joint` flag. Frontend localStorage-backed "Joint filing (×2)" checkbox in Freistellungsauftrag header re-fetches on toggle. 5 unit tests pin the math (single/joint/over-consumed/boundary/zero-income). Live verified: 2023 single 100% → joint 70.86% on 1417.24 €.
- [x] Cost breakdown TER coverage — HandleCosts now also returns `coverage_pct` (% of portfolio value with known TER metadata) and `weighted_ter_covered_only` (normalized TER on the priced subset). Frontend KPI card surfaces an amber `"X.X% priced · Y.YY% on priced"` sub-line when coverage < 99.5%, so partial-metadata portfolios don't silently understate the true TER. 3/3 mock scenarios verified (full / partial / near-full boundary).
- [x] Rolling Volatility clip verified — each window's series naturally starts where the backend can compute a full window (30d / 90d / 365d staggered as expected); switched x-axis from category-union to time + per-series [ts, vol] pairs so mid-range null gaps from differing step rates are gone while leading-edge clipping stays intact; `connectNulls: false` made explicit on each series.
- [x] Max Drawdown KPI now tracks the chart's selected period — previously the KPI was always all-time max while the chart filtered. Derived `windowDDStats` (pct/start/end/days) from `filterByPeriod(drawdown_series, drawdownPeriod)`: pct = abs(window min), end = trough date, start = walk back to most recent drawdown ≥ 0 (prefix `≥` when peak predates the window). Label gains `(1Y)` etc. suffix. Live verified: All `-62.98%`, 1Y `-44.29%`, 6M `-38.12%`, 3M `-4.92%` all match chart minima exactly.
- [x] Risk metrics reconcile with analytics fixtures — HandleRisk forwards `ComputeRiskMetrics` unchanged; verified live values match API JSON exactly. Bumped Volatility + Max Drawdown frontend precision from `toFixed(1)` to `toFixed(2)` (incl. benchmark sub-lines) to match the 2-decimal contract analytics tests already enforce. Sharpe/Sortino were already 2-decimal; VaR stays whole-euro (EUR-denominated, not percent).
- [x] Price alert end-to-end verified — added `Scheduler.RunAlertCheck()` public wrapper + `POST /api/settings/check-alerts` endpoint + "Check Price Alerts" button in Settings so the daily 19:05 cron can be triggered immediately. Live test: price-above alert on GOOG @ 1 EUR threshold (vs. 393.32 actual) → manual trigger → notification appears with message "Alphabet Inc. Class C price reached 393.32 EUR (threshold: 1.00 EUR)" + bell badge shows "1".
- [x] Add goal end-to-end verified — Portfolio Goals form POST /api/settings/goals → 201; goal appears in Portfolio list immediately; navigating to / shows it in Financial Goals strip with progress bar / projected value / status; reload persists; delete cascades to NetWorth on next nav. 10/10 steps clean.
- [x] Time Machine attribution + holdings — routed the change-since-target loop through ClassifyForAttribution + MatchInKindTransferPairs at ScopePortfolio so cash_transfer_* and washed in-kind pairs no longer double-count; market_return is now the residual that satisfies contrib + market + div + int − |fees| − |taxes| = nwChange. Response gains fees/taxes fields. 2 unit tests pin the attribution bucket math and the holdings rebuild on a fixture with deposits/RSU vest/buy-sell/cash-transfer-pair/broker-to-broker-pair/withdrawal.
- [x] Product Switch tax cost uses Freibetrag remaining (not the fixed 1000) — extracted `switchTaxCost(unrealized, teilfreistellung, fsaRemaining)` formula + `remainingFSA(ctx)` helper that mirrors HandleFSAStatus; 6 unit tests pin Abgeltungssteuer rate (26.375%), Teilfreistellung (0.30 equity / 0 bond), FSA depletion paths, and zero-tax-on-loss
- [x] Security Detail sparkline: buy/sell markers snap to nearest trading day — added `snapToSparkline(txnDate)` so a weekend/holiday transaction renders at the next sparkline category (previously silently dropped because category x-axis only matches exact dates). Avg-cost line (gold dashed markLine at total_cost/total_quantity) and legend caption unchanged. Verified live + mocked: Sunday buy at 2026-04-12 now renders on Monday 2026-04-13.
- [x] FX Impact column denominator fixed — total_return % was UnrealizedPL_EUR / cost_native × 100 after the prior EUR-conversion patch, mixing units and propagating into asset_return_pct = total − fx_impact. Now convertToEUR the cost denominator so both sides are EUR; assetReturn algebra holds. Live verification: GOOG row yields 359.1 % total return (plausible for vested RSUs); empty country_weights still gracefully omits the FX fields.
- [x] Holdings sum of market_value = portfolio market_value — HandleHoldings now EUR-converts market_value, unrealized_pl, and the unpriced fallback total via convertToEUR(...,priceRow.Currency); frontend derives totalCostBasis from market_value − unrealized_pl so totalPL is the sum of unrealized_pl (no mixed-currency arithmetic). Live USD holding reconciles to 0.0000 % on live data.
- [x] Compute Trades + Allocate Deposit invariants — extracted HandleRebalance's algorithm into a pure `computeRebalanceTrades` helper; 5 unit tests lock the contract: full rebalance on an 80/20 portfolio with 60/40 target reduces max drift; emits both buy and sell sides; deposit mode (10k) produces only buys even with a heavily overweight leg; deposit allocates proportionally to gap (bigger deficit gets more); balanced portfolio yields zero trades
- [x] Target Allocation contrast — flipped from two-tone bar (amber 0.3-opacity gap rect was barely visible against parchment) to a single bar + target tick: forest bar at actual%, claret if overweight, 2px ink tick with parchment surround at target%. Drift badge dropped broken bg-{color}/20 alpha for visible bg-parchment-deep + border-{color} + text-{color}. Both light + dark mode verified.
- [x] Dividends cumulative chart is monotonic non-decreasing — handler now clamps the increment to ≥0 so a dividend reversal (negative monthly amount) keeps the bar but holds the cumulative line flat; 2 tests cover empty/all-zero/mid-stream/leading/multiple-reversal cases
- [x] Dividends Trailing 12m + Yield on Cost locked — unit tests mirror the handler's strict-After cutoff (row at exact cutoff falls into prior window, not trailing) and the YoC formula trailing/cost × 100; covers boundary, empty, all-old, zero-and-negative-cost-basis cases
- [x] Growth Over Time benchmark line verified — MSCI World legend entry + series conditionally spread on `filtered.some(p => p.benchmark_pct != null)`; absent when backend returns no benchmark, present when it does, ECharts default legend-toggle hides/shows the dotted walnut line; verified across 3 mocked payloads + live data (seeded DB has 1000 points with benchmark_pct populated)
- [x] Health Score weights + band colors locked — backend unit tests assert weights sum to 100, composite = weighted average, and status bands (≥80 good / ≥60 fair / <60 poor); ui-auditor verified circle bg and per-chip text colors track those same bands at all 7 boundary cases (45/59/60/70/79/80/85)
- [x] Portfolio KPI grid reconciles with /api/portfolio/holdings + /performance — fixed silent cost-vs-market mix in headline when only some holdings have prices; three-state label (Market Value / Value (partial market) / Value (Cost)) plus amber `{N}/{total} priced` coverage indicator
- [x] Einlagensicherung indicator at spec thresholds — <90k sage, 90k–100k amber (strict ≤100k upper bound), >100k claret; replaced broken bg-*/10 tints with bg-inset + border-l-[3px] so the severity tint actually renders against this codebase's var-backed Tailwind theme
- [x] AccountCard balance / split fixes — pin formatter to EUR (server returns EUR-converted figures), show Holdings + Cash split on every brokerage even when one side is zero (Morgan Stanley by design); USD-denominated brokerage no longer mislabels the EUR amount with `$`
- [x] Projection FIRE result respects SWR + pension income + marginal tax × tax portion — handler accepts `marginal_rate` / `tax_portion`, inflates annualExpenses to gross (`/ (1 − marginal × portion)`) before SWR sizing; drawdown sim now withdraws gross; frontend passes the user's marginal rate; 5 unit tests lock the formula
- [x] Goals progress bar clamped 0–100% (Math.max+min); native tooltip + ARIA progressbar role with `Estimated MMM YYYY`/`Reached`/`Target MMM YYYY — past due` label derived from months_remaining
- [x] Wealth Waterfall reconciliation — handler now returns `expected_nw` (contrib + market + div + int − fees − taxes) and `reconciliation_gap` (snapshot − expected); added TestWaterfallIdentity covering the classifier-driven aggregation against a synthetic stream
- [x] Freshness indicator: 0d/1d → sage "Up to date"/"Updated yesterday", 2–9d → amber "N days old", ≥10d → claret "Stale — Nd old (date)"; dot + label combo carries non-color cue
- [x] Reconcile hero daily change badge with `/api/portfolio/attribution` — surfaces `Market <signed €> · Cash flow today <signed €>` split below the prose summary when `total_change` materially diverges from total ΔNW; hidden when they agree within €1
- [x] ErrorBoundary regression — verified across 5 scenarios (sync render crash, Try-again recovery, navigation recovery, async errors correctly not caught, repeated crash→recover cycles); added role="alert" and removed orphan top border on the fallback
- [x] Keyboard a11y sweep — added skip-link, swapped mobile JSX so tab order is header→main→bottom-nav, NotificationBell now has aria-expanded/haspopup/controls + Escape restores focus, dynamic aria-label + aria-pressed on dark/privacy toggles, bottom-nav focus ring no longer clips at viewport edge
- [x] Loading / empty / 500 sweep — guarded Analysis crashes on empty allocation/overlap/holdings, optional-chained Settings buildInfo, surfaced HTTP status codes in api/client errors, replaced silent NetWorth 500 with a claret-bordered error block + Retry, added role=status / aria-live on Loading
- [x] Responsive sweep — fixed Settings DangerZone/Passkey overflow, bumped mobile header icons + PeriodSelector + hero PERIODS chips to ≥44x44 touch targets; 15 issues identified, top-5 fixed (rest tracked for follow-up)
- [x] Privacy mode sweep — added CSS rules for native numeric inputs + `.font-mono` ISINs + new `data-privacy="blur"` opt-in attribute; tagged Concentration block, Scheduler messages, HoldingsTable legend
- [x] WCAG AA contrast audit — darkened light-mode `ink-muted` and `amber` tokens; clears 4.5:1 on parchment / parchment-deep / divider; dark mode untouched
- [x] Dark/light mode parity sweep — replaced hardcoded #fff treemap labels in Analysis with theme-swapping `tc.parchment` token; rest of pages/components already use semantic tokens
- [x] Surface single-security >10% net-worth concentration on NetWorth (warning pill with severity-tinted left border)
- [x] Replace account-type binning in NetWorth Asset Allocation donut with holdings-aware breakdown (brokerage cash split, holdings binned by `asset_class`)
- [x] Split Growth Over Time invested series into stacked Cash / In-kind sub-series, legend-toggleable
- [x] Rename "Cash Invested" → "Capital Invested" on Growth Over Time chart; add one-line caption explaining FMV-at-vest valuation
- [x] Add reconciliation test: summary / history / waterfall totals match within €1 on a fixture with RSU vests
- [x] Replace residual `marketReturns = NW − everything_else` in waterfall with explicit monthly holdings price-delta computation
- [x] Wire classifier + pair-matcher into `HandleWealthWaterfall` (waterfall + monthly attribution) — replace ad-hoc switches
- [x] Wire classifier + pair-matcher into `HandlePerformanceHistory` (Growth Over Time) — replace ad-hoc switch
- [x] Wire classifier + pair-matcher into `HandlePerformance` (KPI tiles + IRR + 12m TWR) — replace ad-hoc switches
- [x] Add `analytics.ClassifyForAttribution(txType, hasISIN, scope) Bucket` + tests for all 13 migration-021 tx types
- [x] Add `analytics.MatchInKindTransferPairs` to wash same-ISIN same-qty `transfer`/`transfer_out` pairs (broker-to-broker moves) within ±5 days
- [x] All 56 prior journeys (CSV import, market data, scheduler, net worth, portfolio, analysis, transactions, tax, planning, settings, security; see git history)
